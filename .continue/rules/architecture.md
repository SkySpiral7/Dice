# Project Architecture Overview

- Folder structure is inspired by maven.
- Uses vanilla JS, no frameworks.
- Intended to run in browser though it could also run in node.
- HTML files can `<script id="DiceInclude" src="src/main/javascript/Include.js"></script>`
  as a lazy way to include all js source files under core and statistics.
